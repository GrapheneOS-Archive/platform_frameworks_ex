apply plugin: 'android-library'

android {
    namespace "com.android.common"
    compileSdk 35

    defaultConfig {
        minSdk 35
    }

    sourceSets {
        main {
            java.srcDirs = ['java']
        }
    }

    lintOptions {
        // TODO: fix errors and reenable.
        abortOnError false
    }
}
